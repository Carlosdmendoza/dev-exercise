import Color from 'color';
import { DefaultTheme } from 'styled-components';

// Plain Colors ///
const defaultColor = Color.rgb(10, 10, 10);
const black = Color.rgb(10, 10, 10);
const white = Color.rgb(255, 255, 255);
const mediaPlaceholderGrey = Color.rgb(235, 236, 239);

// Palette Colors ///
const greyDarkest = Color.rgb(20, 20, 20); // this one did not exist in the old theme
const greyDarker = Color.rgb(30, 30, 30);
const grey = Color.rgb(91, 91, 91);
const greyLighter = Color.rgb(153, 153, 153);
const greyLightest = Color.rgb(214, 214, 214);

/// Semantic Colors ///
const primaryDarkest = Color.rgb(0, 23, 113);
const primaryDarker = Color.rgb(0, 37, 181);
const primary = Color.rgb(0, 53, 255);
const primaryLighter = Color.rgb(73, 110, 255);
const primaryLightest = Color.rgb(143, 166, 255);
const primaryBackground = Color.rgb(240, 246, 254);
const secondaryDarkest = Color.rgb(33, 10, 76);
const secondaryDarker = Color.rgb(53, 16, 122);
const secondary = Color.rgb(76, 23, 173);
const secondaryLighter = Color.rgb(127, 89, 196);
const secondaryLightest = Color.rgb(176, 153, 218);
const secondaryBackground = Color.rgb(247, 246, 255);

const successDarkest = Color.rgb(24, 56, 0);
const successDarker = Color.rgb(39, 90, 0);
const success = Color.rgb(56, 128, 0);
const successLighter = Color.rgb(113, 164, 74);
const successLightest = Color.rgb(167, 199, 143);

const noticeDarkest = Color.rgb(153, 111, 0);
const noticeDarker = Color.rgb(217, 157, 0);
const notice = Color.rgb(255, 185, 0);
const noticeLighter = Color.rgb(255, 205, 74);
const noticeLightest = Color.rgb(255, 224, 143);

const errorDarkest = Color.rgb(79, 0, 0);
const errorDarker = Color.rgb(129, 0, 0);
const error = Color.rgb(182, 0, 0);
const errorLighter = Color.rgb(203, 74, 74);
const errorLightest = Color.rgb(222, 143, 143);

/**
 * Default theme for the PHDS.
 */
export const defaultTheme: DefaultTheme = {
    colors: {
        defaultColor,
        black,
        white,
        mediaPlaceholderGrey,

        greyLightest,
        greyLighter,
        grey,
        greyDarker,
        greyDarkest,

        primaryDarkest,
        primaryDarker,
        primary,
        primaryLighter,
        primaryLightest,
        primaryBackground,

        secondaryDarkest,
        secondaryDarker,
        secondary,
        secondaryLighter,
        secondaryLightest,
        secondaryBackground,

        successDarkest,
        successDarker,
        success,
        successLighter,
        successLightest,

        noticeDarkest,
        noticeDarker,
        notice,
        noticeLighter,
        noticeLightest,

        errorDarkest,
        errorDarker,
        error,
        errorLighter,
        errorLightest,
        // Keyword colors
        inherit: 'inherit',
        transparent: 'transparent',
    },
};

// More themes could be added down here (dark, light, etc.)
